
extern crate log;

fn main() {
    env_logger::init();
    error!("Error message");
   // warn!("warning message");
   // info!("information message");
   // debug!("debugging message");
   // println!("Hello, world!");
}
